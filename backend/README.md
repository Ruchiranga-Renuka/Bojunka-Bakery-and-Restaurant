# Bojunka Backend

Backend API using Express and MongoDB (Mongoose). Endpoints under `/api/auth`, `/api/restaurant`, `/api/bakery`.

Quick start:

1. Copy `.env.example` to `.env` and adjust `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies and run:

```bash
cd backend
npm install
npm start
```
